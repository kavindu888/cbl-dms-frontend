import { createPlaceholderPage } from '../createPlaceholderPage'

const DashboardPage = createPlaceholderPage({
  title: 'Dashboard',
  subtitle: 'Operational command centre for daily distribution performance.',
  moduleName: 'Dashboard',
})

export default DashboardPage
