import { createPlaceholderPage } from '../createPlaceholderPage'

const ReconciliationPage = createPlaceholderPage({
  title: 'Collections — Reconciliation',
  subtitle: 'Reconcile expected collections against deposits and variances.',
  moduleName: 'Collections Reconciliation',
})

export default ReconciliationPage
