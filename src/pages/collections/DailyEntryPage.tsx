import { createPlaceholderPage } from '../createPlaceholderPage'

const DailyEntryPage = createPlaceholderPage({
  title: 'Collections — Daily Entry',
  subtitle: 'Capture daily cash and cheque collections by route and collector.',
  moduleName: 'Collections Daily Entry',
})

export default DailyEntryPage
