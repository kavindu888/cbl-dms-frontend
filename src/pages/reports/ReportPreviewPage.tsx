import { createPlaceholderPage } from '../createPlaceholderPage'

const ReportPreviewPage = createPlaceholderPage({
  title: 'Reports — Preview',
  subtitle: 'Preview generated report output, export readiness, and layouts.',
  moduleName: 'Reports Preview',
})

export default ReportPreviewPage
