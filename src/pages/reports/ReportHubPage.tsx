import { createPlaceholderPage } from '../createPlaceholderPage'

const ReportHubPage = createPlaceholderPage({
  title: 'Reports — Hub',
  subtitle: 'Launch and organize analytical reports across the distribution estate.',
  moduleName: 'Reports Hub',
})

export default ReportHubPage
