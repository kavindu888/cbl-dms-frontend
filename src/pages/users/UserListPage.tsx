import { createPlaceholderPage } from '../createPlaceholderPage'

const UserListPage = createPlaceholderPage({
  title: 'User Management — Users',
  subtitle: 'Review user accounts, organization mapping, and access posture.',
  moduleName: 'User Management List',
})

export default UserListPage
