import { createPlaceholderPage } from '../createPlaceholderPage'

const VehicleDetailPage = createPlaceholderPage({
  title: 'Fleet — Vehicle Detail',
  subtitle: 'Inspect a vehicle profile, maintenance posture, and route history.',
  moduleName: 'Fleet Vehicle Detail',
})

export default VehicleDetailPage
