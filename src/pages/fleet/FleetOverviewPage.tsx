import { createPlaceholderPage } from '../createPlaceholderPage'

const FleetOverviewPage = createPlaceholderPage({
  title: 'Fleet — Overview',
  subtitle: 'View vehicle availability, route readiness, and dispatch health.',
  moduleName: 'Fleet Overview',
})

export default FleetOverviewPage
