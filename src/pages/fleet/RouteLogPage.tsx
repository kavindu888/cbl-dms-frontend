import { createPlaceholderPage } from '../createPlaceholderPage'

const RouteLogPage = createPlaceholderPage({
  title: 'Fleet — Route Log',
  subtitle: 'Track trip execution, timeline events, and delivery progression.',
  moduleName: 'Fleet Route Log',
})

export default RouteLogPage
