import { createPlaceholderPage } from '../createPlaceholderPage'

const ReceiptEntryPage = createPlaceholderPage({
  title: 'Purchasing — Receipt Entry',
  subtitle: 'Prepare the receiving workflow for inbound goods and variances.',
  moduleName: 'Purchasing Receipt Entry',
})

export default ReceiptEntryPage
