import { createPlaceholderPage } from '../createPlaceholderPage'

const PurchaseOrderListPage = createPlaceholderPage({
  title: 'Purchasing — Purchase Orders',
  subtitle: 'Monitor supplier orders, lifecycle status, and inbound schedules.',
  moduleName: 'Purchasing Purchase Orders',
})

export default PurchaseOrderListPage
