import { createPlaceholderPage } from '../createPlaceholderPage'
const PurchaseReturnsPage = createPlaceholderPage({
  title: 'Purchasing — Returns',
  subtitle: 'Track supplier return notes, damaged stock, and reconciliation.',
  moduleName: 'Purchasing Returns',
})
export default PurchaseReturnsPage
