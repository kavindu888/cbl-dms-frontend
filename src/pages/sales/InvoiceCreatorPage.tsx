import { createPlaceholderPage } from '../createPlaceholderPage'

const InvoiceCreatorPage = createPlaceholderPage({
  title: 'Sales — Invoice Creator',
  subtitle: 'Launch invoice creation, pricing, and discount workflows.',
  moduleName: 'Sales Invoice Creator',
})

export default InvoiceCreatorPage
