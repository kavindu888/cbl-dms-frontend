import { createPlaceholderPage } from '../createPlaceholderPage'

const InvoiceListPage = createPlaceholderPage({
  title: 'Sales — Invoice List',
  subtitle: 'Track invoice status, dues, and downstream collection readiness.',
  moduleName: 'Sales Invoice List',
})

export default InvoiceListPage
