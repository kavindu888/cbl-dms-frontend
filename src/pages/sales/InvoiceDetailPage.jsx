import { createPlaceholderPage } from '../createPlaceholderPage'
const InvoiceDetailPage = createPlaceholderPage({
  title: 'Sales — Invoice Detail',
  subtitle: 'Inspect a single invoice, allocations, and payment exposure.',
  moduleName: 'Sales Invoice Detail',
})
export default InvoiceDetailPage
